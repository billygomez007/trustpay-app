import { Screen } from './components/screen';

export default function MarketplaceScreen() {
  return (
    <Screen
      eyebrow="Marketplace"
      title="Discover verified commerce."
      description="Marketplace listings, storefronts, and reputation are intentionally postponed until the protected Deal foundation is complete."
    />
  );
}
